Analysis Summary
The MainWindow.xaml.cs file currently handles:
1.	UI Event Handlers - Button clicks, mouse events, keyboard shortcuts
2.	Canvas Rendering - Drawing strokes, rulers, grids, overlays
3.	G-code Generation - Building, exporting, and sending G-code
4.	GRBL Communication - Serial port management and machine control
5.	Project File Management - Save/load operations
6.	Coordinate System Conversions - Doc to bed to work coordinate mapping
7.	Snap/Grid Logic - Endpoint snapping and grid snapping
8.	Theme Management - Window chrome theming
9.	Paint Well UI Management - Paint well selection and property updates
Refactoring Strategy
1. Create a CanvasRenderer Service ✅
Move these methods:
•	RenderAll()
•	DrawRulers()
•	DrawGrid()
•	DrawSafeMarginOverlay()
•	DrawSelectionIndicators()
•	DrawIndicatorsUsingMarkers()
•	RenderReferenceImage()
•	GetHeatMapColor()
•	CreateRulerLabel()
•	UpdateWorkingAreaPreview()
•	RemoveWorkingAreaPreview()
•	Helper methods for rendering
Benefits: Separates rendering logic from UI event handling, makes the rendering pipeline testable.
2. Create a GcodeGenerator Service ✅
Move these methods:
•	BuildGcode()
•	BuildNormalGcode()
•	BuildPaintingModeGcode()
•	BuildPathsWithColorBoundaries()
•	GeneratePaintDipSequence()
•	GenerateWashSwirlPattern()
•	GenerateWipeZigZagPattern()
•	ComputeFit()
•	DocToBed()
•	BedToWork()
•	ClampBedX() / ClampBedY()
•	Fmt()
Benefits: G-code generation logic is completely independent and can be unit tested.
3. Create a CoordinateTransformer Service ✅
Move these methods:
•	DocToBed()
•	BedToWork()
•	ClampBedX() / ClampBedY()
•	MouseToMm()
•	ClampToPage()
•	Coordinate conversion logic
Benefits: Centralizes coordinate system logic, makes it reusable and testable.
4. Expand SnappingService ✅
Create new service for:
•	SnapToEndpoint()
•	SnapToGrid()
•	ApplySnapping()
•	IsSnapEnabled property
•	GetSnapRadius()
•	GetGridSpacing()
•	UpdateSnapIndicator()
•	RemoveSnapIndicator()
•	CalculateSnapPointProgress()
Benefits: Snap logic is complex and deserves its own service.
5. Create a GrblManager Service ✅
Move these methods:
•	LoadGrblSettingsAsync()
•	DisconnectAsync()
•	Connection state management
•	Serial port management logic
•	Bed size and homing configuration
Benefits: Encapsulates all GRBL-specific communication logic.
6. Create a ProjectFileService ✅
Move these methods:
•	SaveProjectToFile()
•	LoadProjectFromFile()
•	NewProjectBtn_Click() logic
•	Serialization/deserialization
Benefits: Separates file I/O from UI logic.
7. Enhance PaintWellController 🔄
Move additional methods:
•	UpdatePaintWellsUI()
•	UpdateSelectedWellPropertiesUI()
•	UpdatePaintWellChipHighlights()
•	Paint well event handlers
•	Property change handlers
Benefits: Makes paint well management fully self-contained.
8. Create a KeyboardShortcutHandler ✅
Move these methods:
•	Window_PreviewKeyDown()
•	Clipboard operations (PerformCopy(), PerformCut(), PerformPaste(), PerformSelectAll())
•	Undo logic (PerformUndo())
Benefits: Centralizes keyboard input handling.
9. Create a ContextMenuController ✅
Move these methods:
•	CanvasContextMenu_Opened()
•	All context menu click handlers
•	Group/ungroup logic
•	Subdivide logic
Benefits: Separates context menu logic from main window.
10. Keep in MainWindow ✅
These should stay:
•	Window lifecycle events (Loaded, SizeChanged)
•	Direct UI element initialization
•	Wiring up controllers/services
•	Simple UI property updates
•	Event handler stubs that delegate to services
Implementation Priority



Phase 1 (High Impact, Low Risk):
1.	CoordinateTransformer - Pure logic, no UI dependencies
2.	GcodeGenerator - Pure logic, easily testable
3.	SnappingService - Self-contained feature
Phase 2 (Medium Impact): 4. CanvasRenderer - Requires careful canvas reference management 5. GrblManager - Consolidates communication logic 6. ProjectFileService - File I/O isolation
Phase 3 (Polish): 7. KeyboardShortcutHandler - Input routing 8. ContextMenuController - Menu logic 9. Enhanced PaintWellController - UI management
Architectural Benefits
1.	Testability: Services can be unit tested in isolation
2.	Maintainability: Each service has a single responsibility
3.	Reusability: Services can be reused in other contexts
4.	Readability: MainWindow becomes a thin orchestration layer
5.	Dependency Injection: Services can be injected for better decoupling
Next Steps
Would you like me to:
1.	Start with a specific service (I recommend CoordinateTransformer or GcodeGenerator first)
2.	Create a detailed implementation plan for one service
3.	Generate the code for a new service class
4.	Show dependency injection setup for the refactored architecture
Let me know which service you'd like to tackle first, and I'll help you implement it!





private void BuildNormalGcode(StringBuilder sb, List<List<LineStroke>> paths, CoordinateTransformService.FitSpec fit, double zUpCmd, double zDownCmd, double feedXY)